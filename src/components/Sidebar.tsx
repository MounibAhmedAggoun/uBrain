import { FilePlus2, Pencil, Trash2 } from 'lucide-react'
import { useRoadmapStore } from '../store/useRoadmapStore'

export function Sidebar({ onNotify }: { onNotify: (message: string) => void }) {
  const { roadmaps, activeRoadmapId, switchRoadmap, createRoadmap, renameRoadmap, deleteRoadmap } = useRoadmapStore()
  return <aside className="sidebar"><div className="sidebar-title"><div><span className="eyebrow">Workspace</span><h2>My roadmaps</h2></div><button className="icon-button" onClick={createRoadmap} aria-label="Create roadmap"><FilePlus2 size={18} /></button></div><div className="roadmap-list">{roadmaps.map((roadmap) => <div className={`roadmap-list-item ${roadmap.id === activeRoadmapId ? 'active' : ''}`} key={roadmap.id}><button className="roadmap-switch" onClick={() => switchRoadmap(roadmap.id)}><span className="roadmap-dot" /> <span>{roadmap.name}</span></button><div className="item-actions"><button onClick={() => { const name = window.prompt('Rename roadmap', roadmap.name); if (name !== null) { renameRoadmap(roadmap.id, name.trim()); onNotify('Roadmap renamed') } }} aria-label={`Rename ${roadmap.name}`}><Pencil size={13} /></button><button onClick={() => { if (window.confirm(`Delete ${roadmap.name}?`)) { deleteRoadmap(roadmap.id); onNotify('Roadmap deleted') } }} aria-label={`Delete ${roadmap.name}`}><Trash2 size={13} /></button></div></div>)}</div><button className="new-roadmap-link" onClick={createRoadmap}>+ New roadmap</button><div className="sidebar-meta">{roadmaps.length} {roadmaps.length === 1 ? 'roadmap' : 'roadmaps'} saved locally</div></aside>
}
